<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title><%= cards.title%></title>

  <link rel="stylesheet" href="/styles/herocard.css">
  <link rel="stylesheet" href="/styles/header.css">
  <link rel="stylesheet" href="/styles/footer.css">

  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.1/css/all.min.css" integrity="sha512-MV7K8+y+gLIBoVD59lQIYicR65iaqukzvf/nwasF0nqhPay5w/9lJmVM2hMDcnK1OnMGCdVK+iQrJ7lzPJQd1w==" crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>
<body>
  <%- include('partials/_header') %>

  <div class="container">

      <div class="cards">
        <img class="card" src="<%= cards.card_front_url %>" alt="photo of card">
        <img src="" alt="stage of card">
      </div>


      <div class="seller-info-container">
        <% if (userID) { %>

          <a href="/admin" id="user-card">
            <img src="https://i.pravatar.cc/55?img=<%= userID %>" alt="user image">
            <p>Jimmy Wright</p>
          </a>

        <% } %>

        <div id="card-info">

          <div id="player-card">
            <h3><%= cards.player_name%></h3>
            <p><%= cards.title %></p>
          </div>

          <div id="description">
            <p class="description-list">Sport: <%= cards.sport%></p>
            <p class="description-list">Brand: <%= cards.brand %></p>
            <p class="description-list">Player: <%= cards.player_name %></p>
            <p class="description-list">Description: <%= cards.description %></p>
          </div>

        </div>

        <div id="price-info">
          <span>$<%= cards.price %>.00</span>
          <div id="favourites">
              <a href="/messages/43" id="contact-btn">Contact Seller</a>
            <% if (!favourites) { %>
              <form action="/herocard/<%= cards.id %>" method="POST" >
                <button id="fav-btn"> Add to favourites </button>
              </form>
            <% } %>
            <% if (favourites) { %>
              <form action="/herocard/remove/<%= cards.id%>" method="POST">
                <button id="remove-btn"> Remove </button>
              </form>
            <% } %>
          </div>
        </div>
      </div>
    <%- include('partials/_footer') %>
  </div>

</body>
</html>
